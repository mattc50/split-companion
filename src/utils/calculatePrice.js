const peopleArray = {}

for person in people {
  peopleArray[person] = 0;
}

for item in items {
  itemArray.push[item]
  const price = item.price;
  const people = item.split.length;
  const split = price / people;
}